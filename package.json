{
  "name": "@kompo/docs",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy": "bun run build && wrangler pages deploy .vitepress/dist --project-name=kompo-docs"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "vitepress-plugin-group-icons": "^1.7.1"
  },
  "devDependencies": {
    "@shikijs/vitepress-twoslash": "^4.0.1",
    "@types/bun": "latest",
    "vitepress": "2.0.0-alpha.16",
    "wrangler": "^4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "packageManager": "bun@1.3.10",
  "engines": {
    "bun": ">=1.3.10"
  },
  "bun": {
    "entry": "index.ts"
  }
}
